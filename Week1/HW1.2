import SwiftUI

struct ContentView: View {

    let name = "Nguyen Van A"
    let studentID = "23110001"
    let age = 18
    let gpa = 8.5
    let isStudent = true

    var body: some View {

        VStack {

            ZStack {

                Rectangle()
                    .fill(Color.blue.opacity(0.2))
                    .frame(height: 180)

                VStack {

                    Image(systemName: "graduationcap.fill")
                        .font(.system(size: 60))
                        .padding()
                        .background(Circle().fill(Color.white))

                    Text(name)
                        .font(.title2)
                        .bold()

                    Text("Always learning, always growing")
                        .foregroundColor(.gray)
                }
            }

            VStack(spacing: 15) {

                HStack {

                    Label(studentID, systemImage: "person.fill")

                    Spacer()

                    Label("\(age)", systemImage: "birthday.cake.fill")
                }

                HStack {

                    Label("\(gpa)", systemImage: "chart.bar.fill")

                    Spacer()

                    Label(
                        isStudent ? "Yes" : "No",
                        systemImage: "graduationcap.fill"
                    )
                }

                Text("Better me, a brighter tomorrow!")
                    .padding()

                Button("Edit Profile") {

                }
                .buttonStyle(.borderedProminent)
            }
            .padding()

            Spacer()

            HStack {

                Spacer()

                VStack {
                    Image(systemName: "house.fill")
                    Text("Profile")
                }

                Spacer()

                VStack {
                    Image(systemName: "book.fill")
                    Text("Subjects")
                }

                Spacer()

                VStack {
                    Image(systemName: "gearshape.fill")
                    Text("Settings")
                }

                Spacer()
            }
            .padding()
        }
    }
}

#Preview {
    ContentView()
}
